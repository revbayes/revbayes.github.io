for (run in 1:4) {
   treetrace = readTreeTrace("output/primates_cytb_JC_run_"+run+".trees", treetype="non-clock")
   # and then get the MAP tree
   map_tree = mapTree(treetrace,"output/primates_cytb_JC_run_"+run+"_MAP.tree")
}

q()
